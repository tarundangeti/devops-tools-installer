apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob
  labels:
    app: cornjob
spec:
  schedule: "*/1 * * * *"
  template:
    metadata:
      labels:
        app: cronjob
    spec:
      conatiners:
        - name: busybox
          image: busybox
          command: ["sh","-c","echo hello from cornJob && sleep 10"]
      restartPolicy: Never
